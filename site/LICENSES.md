# Third-party Licenses

JukuMate LP に同梱・参照している第三者素材のライセンス表記です。

## Icons

Lucide Icons (ISC License) - https://lucide.dev

`site/public/motifs/` 配下のうち下記 SVG は Lucide からダウンロード（または同等デザインを 1.5px stroke / viewBox 24x24 に合わせて再描画）したものです:

- `message-pen.svg`（Lucide `message-square-pen` 派生）
- `receipt.svg`（Lucide `receipt` 派生）
- `folded-paper.svg`（Lucide `file-text` 派生）
- `lightbulb-leaf.svg`（Lucide `lightbulb` + leaf カスタム）

それ以外の `feather.svg` / `eye-soft.svg` / `dot-grid.svg` は Co-Thrive Labs の自前 SVG で、JukuMate LP 内利用に限定して配布します。

## Fonts

- Noto Serif JP / Noto Sans JP / DM Sans (Google Fonts, SIL Open Font License)
